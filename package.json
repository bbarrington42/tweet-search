{
  "name": "tweet-search",
  "version": "1.0.0",
  "description": "Simple tweet search (experimental)",
  "private": true,
  "scripts": {
    "test": "mocha",
    "zip": "rm tweet_search.zip; zip -r tweet-search . -x \".*\""
  },
  "dependencies": {
    "ask-sdk": "^2.3.0",
    "assert": "^1.4.1",
    "dotenv": "^6.2.0",
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}
